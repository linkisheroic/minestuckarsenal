package com.natura.minestuckarsenal.compat.tcon.traits;
/*
import java.util.Random;

import net.minecraft.block.state.IBlockState;
import net.minecraft.entity.EntityLivingBase;
import net.minecraft.item.ItemStack;
import net.minecraft.util.math.BlockPos;
import net.minecraft.util.text.TextFormatting;
import net.minecraft.world.World;
import slimeknights.tconstruct.library.traits.AbstractTrait;

public class TraitCarvable extends AbstractTrait {
	
	public TraitCarvable() {
		super("Carvable", TextFormatting.BLUE);
	}

	@Override
	public void afterBlockBreak(ItemStack tool, World world, IBlockState state, BlockPos pos, EntityLivingBase player,
			boolean wasEffective) {
		super.afterBlockBreak(tool, world, state, pos, player, wasEffective);
		
		Random rand = new Random();
		
		if(rand.nextInt(4) > 3) {
			tool.damageItem(rand.nextInt(4), player);
		}
	}
}
*/
